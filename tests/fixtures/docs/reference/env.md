# Environment

## Environment Variables

`HERMES_HOME` sets the base directory for Hermes state.
`HERMES_DOCS_PATH` overrides where the docs tree is read from.

## Messaging

Hermes supports multiple messaging backends. Configure the default backend
under the `messaging` section of the config file.
